<Window x:Class="ProtoLink.Windows.Messanger.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProtoLink.Windows.Messanger"
        xmlns:views="clr-namespace:ProtoLink.Windows.Messanger.Views"
        mc:Ignorable="d"
        Title="ProtoLink Messenger" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Background="#F0F0F0" Height="40">
            <Button Click="AddContact_Click"
                    HorizontalAlignment="Right"
                    Margin="40,5,5,5"
                    Width="30" Height="30"
                    Content="+"
                    FontSize="18"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTip="Add Contact"/>
            <Button Click="Settings_Click"
                    HorizontalAlignment="Right"
                    Margin="5"
                    Width="30" Height="30"
                    Content="⚙"
                    FontSize="18"
                    Background="Transparent"
                    BorderThickness="0"
                    ToolTip="Settings"/>
        </Grid>

        <Grid Name="MainGrid" Grid.Row="1">
        </Grid>
    </Grid>
</Window>
